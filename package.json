{
  "name": "remote-code-execution",
  "version": "1.0.0",
  "description": "- Docker is operating-system-level virtualization\r - Docker containers OS are based on the local machine OS kernel. Unlike VM docker won't provide isolated OS. All containers share same kernel (Docker vs virtualization)\r - Docker makes it easier to create and deploy applications in an isolated environment.",
  "main": "app.js",
  "scripts": {
    "start": "npx nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "shelljs": "^0.8.4",
    "short-uuid": "^3.1.1",
    "simple-dockerode": "^0.2.2",
    "util": "^0.12.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
